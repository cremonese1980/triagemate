<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="APP_NAME" value="${SPRING_APPLICATION_NAME:-triagemate-ingest}"/>

    <!-- JSON appender (prod/docker) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdc>true</includeMdc>
            <includeContext>false</includeContext>
            <customFields>{"service":"${APP_NAME}"}</customFields>
        </encoder>
    </appender>

    <!-- Plain console appender (dev + test) -->
    <appender name="PLAIN" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%X{requestId}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Dev + Test: plain text console -->
    <springProfile name="dev | test">
        <root level="INFO">
            <appender-ref ref="PLAIN"/>
        </root>
    </springProfile>

    <!-- Docker / Prod: JSON structured logging -->
    <springProfile name="docker | prod">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>

        <!-- Suppress framework noise in production profiles -->
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="org.apache.kafka" level="WARN"/>
    </springProfile>

</configuration>